const BetOptions = (props) => {
    return (
        <form>
            <button>Pot</button>
            <button>Chips</button>
            <button>Half</button>
            <button>Third</button>
            <button>Quarter</button>
            <button>All-In!</button>
        </form>
    );
};

export default BetOptions;